apply plugin: 'java'

repositories {
	mavenCentral()
}

dependencies {
	compile group: 'guava', name: 'guava', version: 'r09'
	compile group: 'jsr', name: 'jsr', version: '3.0.5'
	compile group: 'junit', name: 'junit', version: '3.8.2'
}